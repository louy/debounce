general:
  artifacts:
    - "coverage"

test:
  override:
    - npm test
    - npm run coverage
    - cat ./coverage/lcov.info | ./node_modules/.bin/codacy-coverage

dependencies:
  cache_directories:
    - "node_modules"
  override:
    - npm install
    - npm prune
